name: validate skills

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - run: bash scripts/validate-skills.sh
      - run: node scripts/skill-catalog.js validate

# Nothing Design System — Variants

This file records concrete primitive variants as they are deployed in a project. It starts intentionally small and grows during application.

| Variant | Primitive | Config | Where used |
| --- | --- | --- | --- |
| `instrument-hero-dark` | Hero instrument | Dark default, Doto or Space Grotesk display, one red signal max | TBD |
| `metric-rail-compact` | Data rail | Space Mono values, 12-16px row padding, hairline dividers | TBD |
| `manual-section-light` | Technical section | Light paper surface, black ink, dense table body | TBD |

When a variant is used, add the route, screen, or component path in `Where used` and keep config concrete.
